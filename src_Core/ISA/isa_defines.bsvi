// classic bluespec haskell dont support macro, put them here to fix around
`ifdef RV32
typedef 32 XLEN;

`elsif RV64
typedef 64 XLEN;

`elsif RV128
typedef 128 XLEN
`endif

`ifdef ISA_F

`ifdef ISA_D
typedef 64 FLEN;
`else
typedef 32 FLEN;
`endif

`else
typedef 0 FLEN;
`endif

`define PAT(v, P) \
   case (v) matches \
      tagged P .x: return x; \
   endcase
 
`ifdef INCLUDE_TANDEM_VERIF
Integer kINCLUDE_TANDEM_VERIF = 1;
`else
Integer INCLUDE_TANDEM_VERIF = 0;
`endif